// Faça uma função chamada halfNumber que recebe um parâmetro chamado val
// do tipo number e retorna um number com a metade do valor do parâmetro;


function halfNumber(val) {
    // O parâmetro está retornando um number com o valor reduzido a sua metade;
    return val / 2;
}

// console.log(halfNumber(10));